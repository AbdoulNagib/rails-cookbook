<div id="index_list" class = "container mt-5">
<ul>
  <% @categories.each do |category| %>
    <li><%= link_to category.name, category_path(category) %></li>
  <% end %>
</ul>
</div>

<div id="New_button"class="container mt-5">
<%= link_to "New (category)", new_category_path %>
</div>
